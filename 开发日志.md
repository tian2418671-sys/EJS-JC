# 开发日志

> MVU + EJS 智能代码检查工具（MvuEjsLinter）
> 仓库：https://github.com/tian2418671-sys/EJS-JC

## 2026-09-05（Phase 5：LLM + RAG + 错误解释 v1.0）

### 完成内容
- **新增 `mvu_lint/core/llm_service.py`**（本地 LLM 封装，纯逻辑无 Qt）：
  - 可选依赖 `llama-cpp-python`，延迟 import，未安装时 `available=False`
  - 模型外置：`%APPDATA%/MvuEjsLinter/models/*.gguf`（`find_default_model` 优先 Qwen 命名）
  - `generate()` 支持软超时（daemon 线程 + join），取消事件，加载失败返回 `load_error`
- **新增 `mvu_lint/core/rag_service.py`**（RAG 知识库门面）：
  - 持久化知识库 `%APPDATA%/MvuEjsLinter/knowledge.db`
  - 封装 `VectorDB`（sqlite-vec + NumPy 降级），`search` / `add_chunk` / `chunk_count`
- **新增 `mvu_lint/core/explanation_engine.py`**（错误解释引擎）：
  - 三级策略：RAG 检索 → LLM 生成 → 模板降级（无模型 / 生成失败 / 超时均回退）
  - `ExplanationResult`（source: llm/template，含参考知识块溯源）
  - 中文 prompt 结构化（级别说明 + 错误字段 + 知识片段）
- **`db/database.py`**：新增 `get_error(error_id)` / `set_error_explanation(error_id, text)`
- **`AppController`**：新增 `explain_error` / `get_explanation`，懒加载 `_llm()` / `_rag()`
- **GUI**：新增 `views/explanation_dialog.py`（后台 QThread 生成，显示来源 + 参考知识块）；
  `report_panel` 右键菜单新增「🤖 AI 解释」，`main_window` 接 `explanation_requested` 信号
- **CLI**：新增 `explain` 命令：`python -m mvu_lint explain <角色卡> [错误ID] [--all]`
- **版本号**：`pyproject.toml` 0.1.0 → 1.0.0，「关于」对话框 v0.4 → v1.0
- **文档**：新增 `README.md`（自述页面）

### 测试
- 新增 `tests/test_llm_service.py`（7 项）、`tests/test_explanation_engine.py`（6 项）、
  `tests/test_rag_service.py`（4 项）、`tests/test_app_controller_explain.py`（4 项）
- 全部 headless，无需真实模型（fake LLM / 临时知识库路径隔离）

### 提交
- 待提交：core/llm_service.py、core/rag_service.py、core/explanation_engine.py、
  db/database.py、controllers/app_controller.py、views/explanation_dialog.py、
  views/main_window.py、views/report_panel.py、__main__.py、pyproject.toml、
  README.md、测试、本文档

---

## 2026-09-04（Phase 4：动态定位 + 时间旅行 v0.4）

### 完成内容
- **新增 `mvu_lint/core/debugger.py`**（动态定位器 / F6，纯逻辑无 Qt）：
  - 消费模拟产物（轮次 / 快照 / 命令日志），输出 4 类动态发现：
    - R1 命令执行失败（`status=error`）→ Lv.2
    - R2 变量消失（相邻快照路径丢失/置空）→ Lv.3
    - R3 类型漂移（同路径跨回合类型变化）→ Lv.3
    - R4 未追踪变更（changed_paths 存在但无日志命令解释）→ Lv.3
  - 路径归一化 `norm_path`：JSON Pointer / `stat_data.` / `data.` / `locals.` 前缀
    与 `[N]` 数组访问器统一为快照使用的点分扁平形式
  - 支持传入初始状态（initvar）作为第 0 回合基准，首轮类型漂移/消失可被检出
  - 结果按回合号稳定排序，`analyze()` 可重复（同输入同 ID 同消息）
- **数据库**：新增 `dynamic_findings` 表 + 读写/清空/状态更新接口
  （`get_dynamic_findings` 用 `finding_id AS error_id` 别名，与静态错误字段对齐）
- **`AppController`**：新增 `run_dynamic_analysis` / `get_dynamic_findings` /
  `clear_dynamic_findings` / `set_dynamic_status`；补上缺失的 `has_project()`
- **GUI**：
  - 新增 `TimeTravelDialog`（时间旅行回放 / F5）：滑块 + 上一步/下一步，
    逐回合查看扁平变量表（变更行高亮）、变更 diff、命令日志、完整快照 JSON；
    第 0 步为初始状态（来自卡内「# 变量初始值」块）
  - 新增 `DynamicFindingsDialog`：动态发现与静态报告同风格并列展示，
    支持按级别过滤、双击详情、右键标记状态（待处理/已修复/已忽略）
  - 主窗口新增「时间旅行 — 快照回放」「动态定位（基于日志 + 快照）」按钮，
    模拟运行后启用，导入新卡时重置
- **CLI**：新增 `debug` 命令：`python -m mvu_lint debug <角色卡> [--input 文本 ...]`
  （模拟 → 动态定位 → JSON 输出发现与回合数）
- **测试**：`tests/test_debugger.py`（17 项）、`tests/test_app_controller_dyn.py`（5 项）、
  GUI 冒烟新增 3 项（时间旅行构造/步进、无模拟数据、动态发现对话框）

### 验证
- 完整套件：**149 passed**（128 旧 + 21 新增）
- 端到端 GUI 检查 `scripts/e2e_gui_check.py` 通过
- 真实卡 debug 端到端：鬼 1.2 / 万象1_6 / 星月 / BeiPaiDaoMu / Kora5.3 均可运行
  且 0 误报（真实卡命令多为 `<%_ %>` JS 脚本，规则引擎不执行——Phase 3 边界；
  Kora5.3 的 `<UpdateVariable>` 为文档说明，被 `is_template_text` 正确跳过）
- 合成卡端到端：R1 命令失败（Lv.2）+ R3 类型漂移（Lv.3）均正确检出

### 提交
- 待提交：`mvu_lint/core/debugger.py`、`mvu_lint/db/database.py`、
  `mvu_lint/controllers/app_controller.py`、`mvu_lint/views/time_travel_dialog.py`、
  `mvu_lint/views/dynamic_report_dialog.py`、`mvu_lint/views/main_window.py`、
  `mvu_lint/__main__.py`、测试、本文档

---

## 2026-09-04（Phase 3：模拟对话 + 日志 + 快照 v0.3）

### 完成内容
- **新增 `mvu_lint/core/simulation_engine.py`**：
  - 纯规则驱动的模拟对话引擎（无 LLM，UI 明确标注「技术验证」）
  - 解析 `<!-- mvu: path op value -->` 注释（`=`, `+=`, `-=`）
  - 解析 `<UpdateVariable>` / `<JSONPatch>` JSON Patch 并应用到状态
  - 支持 `@@if getvar('...').includes('...')` 等世界书条目门控激活
  - 每步记录变量快照、变更路径、命令执行日志
- **扩展 `mvu_lint/db/database.py`**：新增 `simulation_logs` 表，补全 `simulation_rounds` / `state_snapshots` 的读写接口
- **扩展 `AppController`**：新增 `run_simulation` / `get_simulation_rounds` / `get_state_snapshots` / `get_simulation_logs` / `clear_simulation`
- **CLI 增加 `simulate` 命令**：`python -m mvu_lint simulate <角色卡> [--input 文本 ...]`
- **GUI 增加「运行模拟（技术验证）」按钮** 和 `SimulationDialog`：展示回合、激活条目、命令、变量快照
- **新增测试**：`tests/test_simulation_engine.py`（9 项）、`tests/test_app_controller_sim.py`（3 项）

### 验证
- 单元测试：12 项新增全绿
- 完整套件：128 passed
- 真实卡（鬼 1.2 版）模拟运行：142 个条目激活、状态稳定、无崩溃

### 提交
- 待提交：`mvu_lint/core/simulation_engine.py`、`mvu_lint/db/database.py`、`mvu_lint/controllers/app_controller.py`、`mvu_lint/__main__.py`、`mvu_lint/views/main_window.py`、`mvu_lint/views/simulation_dialog.py`、测试、本文档

---

## 2026-09-04（Phase 2 后期：`<UpdateVariable>` + JSON Patch 路径校验）

### 完成内容
- **新增 `mvu_lint/core/json_patch.py`**：
  - 提取 `<UpdateVariable>` / `<JSONPatch>` 块（兼容 `<UpdateVariablevariable>`）
  - 解析 RFC 6902 JSON Patch 数组（add / replace / remove / move）
  - JSON Pointer 解码为路径段（`~0`→`~`、`~1`→`/`）并校验格式
  - 路径段映射到 schema 路径树（`.` 连接，`*` 通配数组下标/动态键）
  - 对 concrete patch 做 schema 联动：replace 检查路径存在 + 类型一致；
    add 检查父容器存在；remove 检查路径存在；move 检查 from 与目标父路径
- **集成到 `StaticChecker`**：`check_json_patch(content, file_path, schema_info)`
- **集成到 `AppController.run_static_check`**：检测到 `<UpdateVariable` 即触发 JSON Patch 校验
- **策略保守化**：说明性文字（`[mvu_update]变量输出格式` 等）和模板路径（`${...}`、`<顶层根>` 等）自动跳过，避免真实卡中的格式规则/提示词块误报

### 验证
- 单元测试：`tests/test_json_patch.py` 19 项全绿
- 真实卡端到端：14 张卡里 25 个含 `<UpdateVariable>` 的块，0 误报
- 完整套件：116 passed

### 提交
- 待提交：`mvu_lint/core/json_patch.py`、`mvu_lint/core/static_checker.py`、`mvu_lint/controllers/app_controller.py`、`tests/test_json_patch.py`、本文档

---

## 2026-09-04（Phase 2 收尾扩展：EJS 解析器支持真实卡语法）

### 完成内容
- **`ejs_parser.py` 扩展**：
  - `getvar('X.Y')` 全局引用提取（输出标签 / `<%_` 脚本标签 / `@@if` 宏文本 全覆盖）
  - getvar 路径前缀归一化（剥 `stat_data./data./locals.`），与输出标签一致，联动检查无误报
  - `<%- getvar(...) %>` 标记不安全并触发 Lv.4；`EJSVariableReference` 新增 `source` 字段
- **验证**：真实卡「鬼 1.2版.png」12 个 getvar 引用全部提取，联动检查 0 误报（该卡数据自洽）
- **测试**：88 passed（82 旧 + 6 新增）

### 提交
- 待提交：ejs_parser.py + test_ejs_parser.py + 本文档

---

## 2026-09-04（基于真实卡数据库的算法优化）

### 背景
用户提供 `数据库/`（14 张真实卡）+ 两份指导文件（EJS实战指南、MVU常见问题自查），
要求据此优化程序算法。普查发现三个缺口。

### 完成内容
1. **修复加载崩溃**：真实卡 `extensions.tavern_helper` 可能是 `[["scripts",[...]],["variables",{}]]` 键值对列表
   （非 dict），`_build_blocks` 直接 `th.get("scripts")` 抛 `'list' object has no attribute 'get'`，
   致 3 张卡无法导入。现兼容 dict / 键值对列表两种形态，并对 `character_book`/`regex_scripts` 加防御。
2. **schema 提取泛化**（`extract_schema_dict`）：真实 initvar 有三种形态——
   `# 变量初始值`+缩进 YAML、`[InitVar]` 标签+纯 YAML/`---` YAML、`[InitVar]`+内嵌 JSON（含 `$meta`）。
   现按条目 `comment`/label 识别（`initvar|initialvariables|变量初始值|初始值`），JSON 自动剥 `$meta/$schema`，
   跳过空占位块。修复前 14 张卡仅 1 张有 schema，修复后 4 张（鬼1.2/BeiPaiDaoMu/安达与岛村/星月）。
3. **MVU `[值,说明]` 数组识别**（schema_loader）：`[值, 说明]` 两元素数组被识别为叶子变量，
   类型取第一个元素（如 `["体育馆二楼","说明"]` → string 叶子），而非普通 array。
4. **getvar `[0]` 归一化**（ejs_parser）：MVU 的 `stat_data` 存 `[值,说明]`，`getvar('stat_data.X.Y[0]')`
   末尾 `[0]` 是取值访问器，剥掉后匹配 schema `X.Y`；中间 `[N]` 归一化为 `.N` 配合通配。

### 验证
- 单元测试：97 passed（新增 9：getvar [0]/中间索引、initvar 三种形态、值说明数组、tavern_helper 列表）。
- 真实卡端到端：BeiPaiDaoMu 91 getvar 全部联动通过（schema 77 路径）；安达与岛村正确报出 12 个 Lv.4；
  鬼1.2/星月 0 误报。

### 提交
- 待提交：card_loader.py / ejs_parser.py / schema_loader.py + 测试 + 开发日志

---

## 2026-09-04（Phase 2 完成 + 首次推送）

### 完成内容
- **Phase 2 静态检查器**（`mvu_lint/core/static_checker.py`）
  - MVU 命令格式与路径校验（category=MVU）
  - EJS 变量 ↔ schema.json 联动检查（category=联动）
  - initvar 块完整性及类型一致性检查（category=MVU）
  - schema.json 缺失时进入降级模式：联动/initvar 跳过，MVU 格式校验照常
- **Phase 2 自动修复器**（`mvu_lint/core/auto_fixer.py`）
  - EJS 未闭合标签：自动补 `%>`（`auto_fixable` + `fix_preview` 已落地）
  - initvar 缺字段：按 schema 默认值补齐
  - 修复前生成可读预览，支持一键应用
- **AppController 集成**：`run_static_check` 升级为「EJS + MVU + 联动 + initvar」全量静态检查
- **测试**：新增 `tests/test_static_checker.py`、`tests/test_auto_fixer.py`，与既有套件全绿

### 提交
- `c41a233` Phase 2: add static checker (MVU/linkage/initvar) and auto-fixer
- 本次变更 7 个文件，+1361 / -10 行

### 仓库维护
- 配置远程 `origin → https://github.com/tian2418671-sys/EJS-JC.git`
- **清理历史大文件**：重写历史移除 `dist/`、`build/`、`__pycache__/*.pyc`（约 118MB → 85KB）
- 推送 `master` 成功，4 个提交全部上线

---

## 2026-09-04（Phase 1 中期 + 打磨）

### Phase 1 中期：PyInstaller 打包验证
- 新增 `gui_launcher.py`：打包专用入口，双击 `MvuEjsLinter.exe` 直接启动 GUI
- 修复 `build.spec`：Analysis 入口改为 `gui_launcher.py`；注明 llama-cpp-python 为 Phase 5+ 可选依赖
- 新增 `scripts/verify_binaries.py`：`--verify-binaries` 检查打包产物内 sqlite-vec / PySide6 / llama-cpp-python
- 新增 `scripts/smoke_exe.py`：offscreen 模式 exe 存活冒烟测试
- 结果：`dist/MvuEjsLinter.exe` ~22MB 单文件可执行程序，验证通过

### Phase 1 打磨
- 应用图标（ICO 用 PNG 内嵌格式，Vista+ 兼容，无需第三方库）
- 报告导出（HTML 带 `html.escape` 防注入，error_id 数字序排序避免字典序陷阱）
- 最近文件（QSettings 需显式 `type=list`，否则返回 str 解包报错）
- 错误状态显示

### 提交
- `e20935b` Phase 1 mid: PyInstaller packaging verified; add gui_launcher, smoke/verify scripts
- `97cd8e1` Phase 1 polish: app icon, report export, recent files, error status

---

## 2026-09-03（Phase 0 + Phase 1 主体）

### 完成内容
- **Phase 0 基础库** ✅：4 核心模块（解析器 / 扫描器 / schema 加载 / 检查器）+ CLI + 单元测试
- **Phase 1 GUI 骨架**：
  - PySide6 6.11.2 主窗口：菜单栏、DropZone 拖拽导入、项目信息卡、报告面板、状态栏进度条
  - `mvu_lint/controllers/app_controller.py`：GUI 无关扫描编排层（AppController + ScanSummary）
  - `mvu_lint/controllers/scan_worker.py`：QThread 后台扫描
  - `mvu_lint/views/`：main_window / drop_zone / report_panel / style（深色 QSS）
  - `mvu_lint/app.py`：GUI 入口（`python -m mvu_lint gui`）
- **DB 对齐 v2.1**：`static_errors.is_static`、`schema_cache.source`
- **error_id 全局唯一**：sqlite_sequence 水位跨前缀共享编号线
- **测试**：39 项全绿（含 tests/test_gui_smoke.py offscreen 冒烟 + scripts/e2e_gui_check.py 端到端）

### 关键技术决策（踩坑记录）
1. **QThread 生命周期**：`finished` 先置 `self.worker=None` 再 `connect(deleteLater)`，顺序颠倒会导致 Python 引用悬垂、RuntimeError 被 Qt 吞掉、事件循环永不退出（表现为超时挂死）
2. **跨线程 SQLite**：`sqlite3.connect(check_same_thread=False)`，SQLite serialized 模式允许；读写顺序由 Qt 队列信号保证
3. **next_error_id**：`max(表内后缀, sqlite_sequence 水位)+1`，DELETE 清表后编号不回退
4. **PySide6 安装**：约 150MB，`pip install` 超时阈值 300s 不够，需后台安装；PowerShell 管道把 pip stderr 提示当成 NativeCommandError（returncode 1 假阳性）

### 提交
- `901a4c4` Phase 1: PySide6 GUI 骨架 + ZIP 导入 + EJS 检查面板 + 报告列表

---

## 环境基线

- Windows + Python 3.13.0（D:\Program Files\Python313）
- 依赖：PySide6 6.11.2、sqlite-vec 0.1.9、numpy、pytest 9.1.1
- 打包：PyInstaller（build.spec，产物 ~22MB 单文件）
