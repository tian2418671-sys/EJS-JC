# 后续工作指导

> 依据 `SPEC_v2.md` 第 7 节开发阶段与第 8 节功能清单，结合当前仓库实际进度整理的执行指南。

## 一、当前进度

| 阶段 | 版本 | 状态 |
|:-----|:-----|:-----|
| Phase 0 基础库 | — | ✅ 已完成（4 核心模块 + CLI + 单元测试） |
| Phase 1 GUI + 导入 + 报告 | v0.1 | ✅ 已完成（含打包验证、打磨） |
| Phase 2 完整静态检查 + 自动修复 | v0.2 | ✅ 已完成（2026-09-04 推送） |
| Phase 3 模拟对话 + 日志 + 快照 | v0.3 | ✅ 已完成（2026-09-04） |
| Phase 4 动态定位 + 时间旅行 | v0.4 | ⬜ 待启动 |
| Phase 5 LLM + RAG + 错误解释 | v1.0 | ⬜ 待启动 |
| Phase 6 打包发布 + 文档 | v1.1 | ⬜ 待启动 |

## 二、Phase 2 收尾清单（低优先级，可随时补）

- [ ] GUI 上给静态检查结果接「自动修复」按钮（后端 `AutoFixer` 已就绪，需在 report_panel 加交互）
- [ ] 静态检查结果新增列展示 `fix_preview`，双击触发一键修复
- [ ] 确认 `run_static_check` 降级模式（无 schema.json）在 GUI 状态栏有提示文案
- [ ] 补 1–2 个修复后「重新扫描」的端到端用例

## 三、Phase 3：模拟对话 + 日志 + 快照（v0.3） ✅ 已完成

对应功能：F4（模拟对话引擎，技术验证）、F5（日志追踪与变量快照）

### 已完成
1. **规则驱动模拟引擎**：`mvu_lint/core/simulation_engine.py`
2. **对话/回合落库**：`simulation_rounds` 表；命令日志 `simulation_logs` 表；变量快照 `state_snapshots` 表
3. **GUI 入口**：`SimulationDialog` + 主窗口「运行模拟（技术验证）」按钮
4. **CLI 命令**：`python -m mvu_lint simulate <角色卡>`

### 待 Phase 4 扩展
- 时间旅行回放 UI（滑块 / 快照 diff）
- 动态定位器：基于日志 + 快照 diff 定位变量异常变更点
- 与静态错误并列展示

## 四、Phase 4：动态定位 + 时间旅行（v0.4）

## 四、Phase 4：动态定位 + 时间旅行（v0.4）

对应功能：F6（动态定位器）、F5（时间旅行部分）

### 建议拆解
1. **动态定位器**：基于运行日志 + 快照 diff 定位变量异常变更点
2. **时间旅行**：快照回放 UI（滑块按步回退/前进，查看任意步骤变量状态）
3. **与静态结果打通**：动态定位结果与 Phase 2 静态错误在报告中并列展示

## 五、Phase 5：LLM + RAG + 错误解释（v1.0）

对应功能：F7（AI 错误解释 + RAG 知识库）

### 模型选择（见 SPEC 附录 A）
| 模型 | 参数量 | 量化格式 | 用途 |
|:-----|:-------|:---------|:-----|
| Qwen2.5-1.5B-Instruct（默认） | 15亿 | Q4_K_M | 模拟对话 + 错误解释 |
| Qwen2.5-3B-Instruct（可选升级） | 30亿 | Q4_K_M | 高质量错误解释（手动切换） |

下载源：HuggingFace / ModelScope。**模型外置**，不随安装包分发。

### 注意
- llama-cpp-python 为 Phase 5+ 可选依赖，当前未安装；打包验证时保持剥离
- 知识库走 sqlite-vec（`%APPDATA%/MvuEjsLinter/knowledge.db`），内置 NumPy 降级路径
- 错误解释生成要做超时与取消控制，避免 GUI 卡死（参考 QThread 生命周期经验）

## 六、Phase 6：打包发布 + 文档（v1.1）

- [ ] 更新 build.spec 入口与依赖清单（确认 Phase 5 可选依赖不进入核心包）
- [ ] 编写用户手册（导入 → 检查 → 修复 → 导出 全流程）
- [ ] 发布说明 + 版本号规范（v0.1 → v1.1 对齐 SPEC）
- [ ] 最终体积基线：接受 ~200MB（模型外置前提），核心库剥离 matplotlib/scipy 等非必要依赖

## 七、工程规范（长期约束）

### Git 纪律
- **严禁提交** `build/`、`dist/`、`__pycache__/`、`*.pyc`（.gitignore 已覆盖；本地可保留用于打包）
- 提交信息用「阶段 + 变更摘要」格式，如 `Phase 2: add static checker (MVU/linkage/initvar) and auto-fixer`
- 推送前 `git status` 检查是否有大文件混入；仓库历史已清理过一次（118MB → 85KB），避免复发

### 质量要求
- 每次功能变更必须带测试，合并前测试全绿（当前 39+ 项）
- GUI 相关改动跑 `scripts/e2e_gui_check.py` 端到端
- 打包相关改动跑 `scripts/verify_binaries.py` + `scripts/smoke_exe.py`

### 关键技术红线（历史踩坑，勿重蹈）
1. QThread `finished` → 先置空引用 → 再 `deleteLater`，顺序不可颠倒
2. SQLite 跨线程用 `check_same_thread=False`，读写顺序靠 Qt 队列信号保证
3. error_id 用 sqlite_sequence 水位，清表后不回退
4. QSettings 读 list 必须显式 `type=list`
5. 报告导出（HTML）对 file_path/message/suggestion 做 `html.escape`，防角色卡注入
6. 大依赖安装（如 PySide6 150MB）走后台安装，超时阈值放宽

## 八、已知风险与缓解（SPEC 第 9 节摘要）

| 风险 | 缓解 |
|:-----|:-----|
| sqlite-vec 扩展打包失败 | 内置 NumPy 降级路径；Phase 1 中期已做打包验证 ✅ |
| 1.5B 模拟对话质量低 | UI 写「技术验证」；系统提示词强约束 + few-shot；3B 可选升级 |
| schema.json 双形态解析误判 | 无法识别时输出警告并降级，报告中标注「未完全解析」 |
| EJS 静态子集边界漏报 | 仅 `is_static=True` 报联动错误，其余统计并提示人工确认 |
| 打包体积超 200MB | 模型外置；剥离非必要依赖；接受 200MB 基线 |
